기지개 {
    // 파이썬 코드를 그대로 써도 돌아갑니다 (들여쓰기만 맞으면)
    import random

    야옹("이번 주 로또 번호를 점지해주겠다냥!")
    
    박스 횟수 = 0
    꾹꾹이 (횟수 < 6) {
        박스 번호 = random.randint(1, 45)
        야옹("번호: ", 번호)
        횟수 = 횟수 + 1
    }
} 식빵굽기 